import { Plus, ArrowUpRight, Link as LinkIcon } from 'lucide-react';
import { SourceNote, CopyAction } from '@/components/Folio';

export const PublicationRow = ({ item: p }) => <li className="publication-record" id={p.id} data-testid={`pub-row-${p.number}`}><span className="record-year">{p.year}</span><article><p className="record-type">{p.type}</p><h2 className="record-title">{p.title}</h2><p className="record-authors">{p.authors.length > 220 ? `${p.authors.slice(0, p.authors.lastIndexOf(',',200))}, et al. (full author list below)` : p.authors}</p><p className="record-citation">{p.citation}</p><div className="record-actions"><a href={p.url} target="_blank" rel="noreferrer" data-testid={`publication-link-${p.number}`}>{p.doi ? 'Read publication' : 'Publisher reference'}<ArrowUpRight size={13} /></a><CopyAction text={`${p.authors} ${p.title}. ${p.citation}${p.doi ? ` DOI: ${p.doi}` : ''}`} id={`copy-citation-${p.number}`} /><a href={`#${p.id}`} aria-label={`Link to publication ${p.number}`} data-testid={`publication-anchor-${p.number}`}><LinkIcon size={13} />Record link</a></div><details className="record-details" data-testid={`pub-details-${p.number}`}><summary data-testid={`pub-expand-${p.number}`}>Full record & provenance<Plus size={13} /></summary><div className="record-expanded" data-testid={`pub-expanded-${p.number}`}><h4>Complete author list</h4><p>{p.authors}</p>{p.corresponding && <p>* The source identifies Peddy Vishweshwar as a corresponding author.</p>}{p.doi && <p>DOI: {p.doi}</p>}<SourceNote id={`pub-${p.number}`} pages={p.source.pages.join('–')}>Numbered publication entry {p.number}. Transcribed from the supplied June 2026 annexure. Source spellings and bibliographic details are retained.</SourceNote><details className="raw-source"><summary data-testid={`pub-transcription-${p.number}`}>View the source transcription</summary><p className="raw-record" data-testid={`pub-source-text-${p.number}`}>{p.sourceText}</p></details></div></details></article></li>;