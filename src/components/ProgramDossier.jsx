import { SourceNote, TextLink } from '@/components/Folio';

export const ProgramDossier = ({ program: p, index }) => <article id={p.id} className="program-dossier" data-testid={`program-${p.id}`}><span className="dossier-number">{String(index+1).padStart(2,'0')}</span><div><h3>{p.api}</h3><p className="form-name">{p.form}</p><p className="body-copy">{p.description}</p><SourceNote id={`program-${p.id}`} pages={p.page}>{p.note || 'Transcribed from the public-domain program table. No additional clinical or commercial outcomes are inferred.'}</SourceNote></div><dl className="record-ledger" data-testid={`program-record-${p.id}`}><dt>Development record</dt><dd>{p.record}{p.dmf && <span>US-DMF {p.dmf}</span>}</dd>{p.patents.length > 0 && <><dt>Related intellectual property</dt><dd>{p.patents.map(n => <span key={n}>{n}</span>)}</dd></>}{p.patentId && <dd><TextLink to={`/patents#patent-${p.patentId}`} id={`program-patent-${p.id}`}>Read the patent record</TextLink></dd>}</dl></article>;