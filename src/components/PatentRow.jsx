import { Plus, Link as LinkIcon } from 'lucide-react';
import { SourceNote, CopyAction } from '@/components/Folio';

export const PatentRow = ({ item: p }) => <li className="publication-record" id={p.id} data-testid={`patent-row-${p.number}`}><span className="record-year">{p.year || '—'}</span><article><p className="record-type">{p.jurisdiction} · {p.type}</p><h2 className="record-title patent-title">{p.title}</h2><p className="patent-identifier">{p.identifier}</p><p className="record-citation">Source entry {p.number} · {p.year ? p.dateType : 'No standalone date established for this entry'}</p><div className="record-actions"><a href={`#${p.id}`} data-testid={`patent-anchor-${p.number}`}><LinkIcon size={13} />Record link</a><CopyAction id={`copy-patent-${p.number}`} label="Copy reference" text={`${p.title}. ${p.identifier}. ${p.inventors}`} /></div><details className="record-details" data-testid={`patent-details-${p.number}`}><summary data-testid={`patent-expand-${p.number}`}>Inventors, documents & source<Plus size={13} /></summary><div className="record-expanded" data-testid={`patent-expanded-${p.number}`}><h4>Co-inventors, as listed</h4><p>{p.inventors}</p><h4 style={{marginTop:25}}>Document references</h4><ul className="documents-list">{p.documents.map((d,i)=><li key={i}>{d}</li>)}</ul>{[7,8].includes(p.number) && <p style={{marginTop:18}}>AU2014259029 B2 appears in both Nilotinib source entries. It is not counted here as two unique Australian grants.</p>}{p.number===14 && <p style={{marginTop:18}}>The program table on page 2 lists WO 2023/223253 A1, while this entry lists WO 2023/223251 A1. No equivalence is assumed.</p>}<SourceNote id={`patent-${p.number}`} pages={p.source.pages.join('–')}>The status and identifiers are reported by the supplied annexure; this is not a current legal-status assessment. Related documents are preserved as source references, not independently established patent families.</SourceNote><details className="raw-source"><summary data-testid={`patent-transcription-${p.number}`}>View the source transcription</summary><p className="raw-record" data-testid={`patent-source-text-${p.number}`}>{p.sourceText}</p></details></div></details></article></li>;